*.lgb -text

# Generated by scripts/audit_export_s3_wind_input_20260824.py and SHA-asserted by
# notebooks/audit_s3_anchor_scaffold.ipynb cell 1. Written LF; -text keeps it LF
# through commit and checkout under core.autocrlf=true. The other three files that
# cell 1 asserts are pre-existing tracked artifacts and are deliberately NOT listed
# here: the notebook asserts their LF-normalised content instead, so their committed
# bytes are left exactly as they were. Added 2026-08-24.
data/audit_s3_wind_input_cell63_20260824.csv -text

# Generated by scripts/audit_export_tierB_anchors_126_20260825.py (audit v2 Tier B
# anchors 1, 2, 6) and hashed in docs/audit/extracts/README.md. Written CRLF by
# pandas to_csv / by byte copy of a CRLF working copy; -text keeps those exact
# bytes through commit and checkout under core.autocrlf=true, so the README SHAs
# are properties of the content and not of this machine. Same reasoning as the
# 2026-08-24 entry above. Added 2026-08-25.
docs/audit/extracts/revenue_by_strategy_1460h.csv -text
docs/audit/extracts/bonus_revenue_inputs_2019-03.csv -text
docs/audit/extracts/tennet_penalty_alpha_star_derivation.csv -text

# The recorded pip_freeze_sha256 in reports/audit_anchor6_s3_case1_20260825.json
# is the hash of this file's LF bytes. -text keeps disk bytes equal to committed
# bytes under core.autocrlf=true, so the two stay identical on any clone.
# Added 2026-08-25.
reports/audit_anchor6_pipfreeze_20260825.txt -text

# Files whose SHA-256 is recorded elsewhere and must therefore survive a clone
# byte-for-byte under core.autocrlf=true. Same reasoning as the 2026-08-24 and
# 2026-08-25 entries above. Added 2026-08-27.
#
#   data/submission/submission.json          -> SHA in reports/task2_submission_build_20260827.json
#                                               and in the 2026-08-27 report-back
#   data/iea22_280_rwt/...wisdem_power_ct.csv -> SHA in its own .provenance.json
data/submission/submission.json -text
data/iea22_280_rwt/iea22_280_rwt_wisdem_power_ct.csv -text
